<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bengebackend.mapper.ScriptMapper">

    <resultMap id="BaseResultMap" type="com.bengebackend.model.Script">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="is_deleted" property="isDeleted" jdbcType="BOOLEAN"/>
        <result column="last_updated" property="lastUpdated" jdbcType="TIMESTAMP"/>
        <result column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="stage" property="stage" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, content, is_deleted, last_updated, user_id, stage
    </sql>

    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM script 
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByUserId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM script 
        WHERE user_id = #{userId} AND is_deleted = 0 
        ORDER BY last_updated DESC
    </select>

    <insert id="insert" parameterType="com.bengebackend.model.Script" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO script (
            title, content, user_id, is_deleted, last_updated, stage
        ) VALUES (
            #{title}, #{content}, #{userId}, #{isDeleted}, #{lastUpdated}, #{stage}
        )
    </insert>

    <update id="update">
        UPDATE script 
        SET title = #{title}, 
            content = #{content}, 
            stage = #{stage}, 
            last_updated = #{lastUpdated}
        WHERE id = #{id}
    </update>

    <update id="deleteById" parameterType="java.lang.Integer">
        UPDATE script SET is_deleted = 1 WHERE id = #{id}
    </update>

</mapper>
